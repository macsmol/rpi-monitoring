TODO:
    -periodically delete old videos from disk
    -BUG:investigate script occasionally silently stop to work (got it into state where no notifications get triggered though the script process is running; no exceptions were logged: some infinite loop?)
v2: 
    -integration with Proton Drive CLI
    -


Extras:
-fallback email account?
WONT DO:
-GPG: fix Proton mail app not automatically-deciphering pgp-encrypted msgs; 
Protonmail seems alter the incoming msg format. The mime headers in my protonmail inbox differ from the ones that left the python script.
Result is that I see cryptogram as a string in my mailbox - rather than deciphered attachment as I was hoping.

DONE:
-tweak picamera no to not miss the first frames (on destination hardware)
-sometimes file is not created/flushed at the moment when we start sending email; waiting in a loop for file to be created does not help